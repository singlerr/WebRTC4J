1c1
< cd sr   /*
---
> /*
90a91,100
>   scoped_refptr(const T* ptr, int size, void *owner) : ptr_(ptr), size(size), owner(owner) {
>       if (ptr_)
>           ptr_->AddRef();
>   }
> 
>   static void deallocate(void* owner) {
>     auto ptr = (scoped_refptr<T>*) owner;
>     ptr->Release();
>   }
> 
163a174,175
>   int size;
>   void* owner;
